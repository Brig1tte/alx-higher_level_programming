#!/usr/bin/node
// a script to print 3 lines by using an array of string and a loop
const lines = ['C is fun', 'Python is cool', 'Javascript is amazing'];
for (let j = 0; j < lines.length; j++) {
  console.log(`${lines[j]}`);
}
