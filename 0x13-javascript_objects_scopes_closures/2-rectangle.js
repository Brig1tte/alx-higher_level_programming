#!/usr/bin/node
// a class Rectangle that defines a rectangle:
class Rectangle {
	constructor(h, w) {
		this.height = h;
		this.width = w;
	}
}

if (w >= 0 || h >= 0) {
		const rectangle = {};
	}
}

module.exports = Rectangle
