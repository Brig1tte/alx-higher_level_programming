-- A Script to list all rows of the first_table from the database hbtn_0c_0 in MySQL server
-- All fields should be printed
-- Database name is passed as an arg of the mysql command

SELECT * FROM first_table;
